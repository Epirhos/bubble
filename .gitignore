# Gradle / build
.gradle/
build/
*/build/
**/build/

# Config machine-spécifique (chemin SDK Android local — ne jamais committer)
local.properties

# Kotlin/Native & Xcode
*.xcodeproj/
iosApp/Vendor/
DerivedData/
*.xcuserstate

# Modèles ML volumineux (~11 Mo) — récupérés par ./scripts/fetch-models.sh
androidApp/src/main/assets/*.task

# Node (serveur de signalisation)
node_modules/

# IDE / OS
.idea/
.DS_Store
*.iml

# Caches Kotlin locaux (logs d'erreurs / sessions du daemon)
.kotlin/

# Archives téléchargées par la CI iOS (WebRTC.xcframework)
webrtc.zip
